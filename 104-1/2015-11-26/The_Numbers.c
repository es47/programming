#include <stdio.h>
main()
{
    int a[7], t, i, n, m;
    while (scanf("%d %d", &n, &m) != EOF)
    {
        a[6] = m % 10;
        a[5] = (m % 100 - m % 10) / 10;
        a[4] = (m % 1000 - m % 100) / 100;
        a[3] = (m % 10000 - m % 1000) / 1000;
        a[2] = (m % 100000 - m % 10000) / 10000;
        a[1] = (m % 1000000 - m % 100000) / 100000;
        a[0] = (m % 10000000 - m % 1000000) / 1000000;
        t = 0;
        for (i = 0; i < 6; i++)
            if ((n == a[i] * 10 + a[i + 1]))
                t++;
        printf("%d\n", t);
    }
    return 0;
}
